package io.github.lingqiqi5211.crashcatcher.bridge;

/**
 * The bridge's version, reported in its hello frame.
 *
 * A placeholder. cch-packager replaces this file with one carrying the same label it writes into
 * module.prop, so the diagnostics page can tell a stale dex from a current one. Java has no
 * preprocessor, which is why this is a file rather than a build flag.
 */
final class BridgeVersion {
    static final String VALUE = "dev";

    private BridgeVersion() {}
}
