<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Notifications are posted by the privileged bridge inside the framework, not by
      this app, so no POST_NOTIFICATIONS is requested here. The permission is
      declared only for the fallback path used while the bridge is unavailable.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Resolving package labels and icons for the apps screen. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"
        xmlns:tools="http://schemas.android.com/tools" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrashCatcher">

        <!--
          `singleTop` so a crash alert or a notification opening a record reaches the
          running instance through `onNewIntent`. Without it the second one builds another
          instance, and the one the user is looking at keeps showing the previous crash.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.CrashCatcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The crash alert, and where notification taps land.

          `exported` because the callers are outside this app: the daemon's privileged
          bridge starts it by component name for 弹窗 mode, and the PendingIntents on a
          notification are fired by the system. The component name below is the one the
          daemon has hardcoded — renaming it silently breaks 弹窗 mode.

          `singleTask` so a second crash reuses the instance instead of stacking alerts,
          and `excludeFromRecents` because an interruption over another app should not
          leave a card in the recents list.
        -->
        <activity
            android:name=".ui.detail.CrashDetailActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.CrashCatcher.Alert">
            <intent-filter>
                <action android:name="io.github.lingqiqi5211.crashcatcher.BRIDGE_ACTION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          Serves a log on its way to another app.

          Sharing a large trace cannot go through `EXTRA_TEXT`: a tombstone or an ANR dump
          runs past what a binder transaction carries, and `startActivity` throws rather
          than truncating. Past that size the log is written to the cache and handed over
          as a content Uri instead.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.shares"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>

    </application>

</manifest>
